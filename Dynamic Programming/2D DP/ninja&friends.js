class Solution {
    calcMax(i1,j1,j2,grid){
      if(j1 < 0 || j > m || )
    }
    maxChocolate(grid) {
        this.calcMax(0,0,0)
    }
}

let grid = [[4, 1, 2], [3, 6, 1], [1, 6, 6], [3, 1, 2]];
let sol = new Solution()
sol.maxChocolate(grid)